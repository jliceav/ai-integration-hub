# Copia este archivo como .env y cambia los valores. NUNCA subas .env a GitHub.
ENVIRONMENT=local
DATABASE_URL=sqlite:///./hub.db
API_KEY=cambia-esta-llave
WOOCOMMERCE_WEBHOOK_SECRET=el-secreto-que-pongas-en-woocommerce

# mock = sin costo (reglas). Para IA real: anthropic u openai + tu llave
AI_PROVIDER=mock
AI_API_KEY=
# Opcional. Revisa el nombre de modelo vigente en la documentación del proveedor.
AI_MODEL=
